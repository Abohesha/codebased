=== Cell 2 ===
def main(session: snowpark.Session):
    """
    Main function for Snowflake worksheet execution - Phase 3 Advanced Analytics
    """
    try:
        for day in range(28, 15, -1):
            target_date = f'2026-02-{day:02d}'
            print(f"≡ƒÄ» Target date: {target_date}")
            result = phase3_advanced_analytics_processor(session, target_date)
            print(result['summary'])
    except Exception as e:
        error_report = format_error_details(e, "MAIN FUNCTION - PHASE 3")
        print(f"""Γ¥î PHASE 3 CRITICAL FAILURE:
    {error_report}""")  
 
# Configuration for output tables
MASTER_METRICS_TABLE = "EVALS_SUMMARY_2_test_only"
TEST = False
DEPARTMENT_FILTER = "AT_Filipina_Outside_UAE"
main(session)

# ΓöÇΓöÇ Average Message Delay Analysis ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
MESSAGE_DELAY_DATES = [
    '2026-05-04',   # ΓåÉ Day 1: edit as needed
    '2026-05-05',   # ΓåÉ Day 2: edit as needed
    '2026-05-06',   # ΓåÉ Day 3: edit as needed
]
delay_results_df = run_mv_message_delay_analysis(session, MESSAGE_DELAY_DATES)
display(delay_results_df[['CONVERSATION_ID', 'DATE', 'TIME_DIFF_SECONDS']])

=== Cell 3 ===
target_date = (datetime.now() - timedelta(days=1)).strftime('%Y-%m-%d')
Updated_at_date=(datetime.now()).strftime('%Y-%m-%d')


=== Cell 29 ===
# Initialize the client for chat analysis
# targetDate = (datetime.now() - timedelta(days=1)).strftime('%Y-%m-%d')
client = SnowflakeAnalyticsClient(
    target_date=target_date,
    department_name='MV_Resolvers',
    table_name='LLM_EVAL.RAW_DATA.MV_CLIENT_CHATS',
    bot_skills=['GPT_MV_RESOLVERS'],
    agent_skills=['MV_RESOLVERS_SENIORS', 'MV_CALLERS', 'MV_RESOLVERS_MANAGER', 
                           'GPT_MV_RESOLVERS_SHADOWERS', 'GPT_MV_RESOLVERS_SHADOWERS_MANAGER','Pre_R_Visa_Retention'],
    preprocessing_filters=[1, 2, 3, 4, 5,6,7,8,9]  # Chat filters
)

=== Cell 34 ===
# results = get_mv_resolvers_metrics(session, '2026-02-06')
    
# # Access the conversation ID sets
# total_convs = results['chats_supposed_to_be_handled_by_bot']
# agent_intervened = results['chats_with_at_least_1_agent_message']
    
# print(f"Total conversations: {len(total_convs)}")
# print(f"Conversations with agent messages: {len(agent_intervened)}")
    
#     # Get bot-handled conversations (set difference)
# bot_handled = total_convs - agent_intervened
# print(f"Bot-handled conversations: {len(bot_handled)}")

=== Cell 35 ===
# ΓöÇΓöÇ Run Code-Based Evals: multiple_contract_detector ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
# Metrics computed (code-based only, no agent-related):
#   1. Wrong Tool Calls       ΓåÆ GUARDRAIL_STOPPED_TOOLS
#   2. Missed Tool Calls      ΓåÆ GUARDRAIL_MISSED_TOOLS
#   3. False Promises         ΓåÆ GUARDRAIL_FALSE_PROMISE_NO_TOOL
#   4. Frustration Transfers  ΓåÆ raw chat table (flag: "Transfer due to frustration")

dept_mcd = 'multiple_contract_detector'
target_date_mcd = (datetime.now() - timedelta(days=1)).strftime('%Y-%m-%d')
filter_date_mcd = (datetime.strptime(target_date_mcd, '%Y-%m-%d') + timedelta(days=1)).strftime('%Y-%m-%d')

print(f"≡ƒÜÇ Running code-based evals for: {dept_mcd}")
print(f"≡ƒôà Target date: {target_date_mcd}")
print("=" * 55)

departments_config_mcd = get_snowflake_departments_config()
dept_config_mcd = departments_config_mcd[dept_mcd]

# Phase 1: Load & filter conversations
filtered_df_mcd, stats_mcd, success_mcd, _ = process_department_phase1(
    session, dept_mcd, target_date_mcd
)

if success_mcd and not filtered_df_mcd.empty:
    # Guardrail analyses ΓÇö saves results to the three GUARDRAIL_* tables
    wrong_tools_mcd   = analyze_guardrail_stopped_tools(session, filtered_df_mcd, dept_mcd, target_date_mcd)
    missed_tools_mcd  = analyze_guardrail_missed_tools(session, filtered_df_mcd, dept_mcd, target_date_mcd)
    false_promise_mcd = analyze_guardrail_false_promise_no_tool(session, filtered_df_mcd, dept_mcd, target_date_mcd)

    # Supporting tables (needed for guardrail summary by category)
    analyze_conversation_categories(session, filtered_df_mcd, dept_mcd, target_date_mcd)
    analyze_conversation_tool_calls(session, filtered_df_mcd, dept_mcd, target_date_mcd)
    generate_guardrail_summary_by_category(session, dept_mcd, target_date_mcd)

    # Frustration transfers ΓÇö flag: "Transfer due to frustration" in raw chat table
    frustration_q_mcd = f"""
    SELECT COUNT(DISTINCT CONVERSATION_ID) as FRUSTRATION_COUNT
    FROM {dept_config_mcd['table_name']}
   

=== Cell 36 ===
import streamlit as st

# ΓöÇΓöÇ Config ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
_dept     = 'multiple_contract_detector'
_tdate    = (datetime.now() - timedelta(days=1)).strftime('%Y-%m-%d')
_fdate    = (datetime.now()).strftime('%Y-%m-%d')   # UPDATED_AT is 1 day ahead
_tbl      = 'SILVER.CHAT_EVALS.MV_CLIENTS_CHATS'
_skill    = 'MULTIPLE_CONTRACT_DETECTOR'

# ΓöÇΓöÇ Helper ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
def _count(query):
    try:
        df = session.sql(query).to_pandas()
        return int(df.iloc[0, 0]) if not df.empty else 0
    except Exception as ex:
        st.warning(f"Query error: {ex}")
        return 0

def _fetch(query):
    try:
        return session.sql(query).to_pandas()
    except Exception as ex:
        st.warning(f"Query error: {ex}")
        return pd.DataFrame()

# ΓöÇΓöÇ Metric queries ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
wrong_tools_count = _count(f"""
    SELECT COUNT(DISTINCT CONVERSATION_ID)
    FROM LLM_EVAL.PUBLIC.GUARDRAIL_STOPPED_TOOLS
    WHERE DATE = '{_tdate}' AND DEPARTMENT = '{_dept}'
""")

missed_tools_count = _count(f"""
    SELECT COUNT(DISTINCT CONVERSATION_ID)
    FROM LLM_EVAL.PUBLIC.GUARDRAIL_MISSED_TOOLS
    WHERE DATE = '{_tdate}' AND DEPARTMENT = '{_dept}'
""")

false_promise_count = _count(f"""
    SELECT COUNT(DISTINCT CONVERSATION_ID)
    FROM LLM_EVAL.PUBLIC.GUARDRAIL_FALSE_PROMISE_NO_TOOL
    WHERE DATE = '{_tdate}' AND DEPARTMENT = '{_dept}'
""")

frustration_count = _count(f"""
    SELECT COUNT(DISTINCT CONVERSATION_ID)
    FROM {_tbl}
    WHERE DATE(UPDATED_AT) = '{_fdate}'
      AND UPPER(TEXT) LIKE '%TRANSFER DUE TO FRUSTRATION%'
      AND UPPER(TARGET_SKILL_PER_MESSAGE) = '{_skill}'
""")

# ΓöÇΓöÇ Detail tables ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
df_wrong = _fetch(f"""
    SELECT TOOL_NAME, EVALUATOR_REASON,
           COUNT(DISTINCT CONVERSATION_ID) AS CONVERSATIONS
    FROM LLM_EVAL.PUBLIC.GUARDRAIL_STOPPE

=== Cell 37 ===
# ΓöÇΓöÇ Run DDC Analytics Pipeline ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
# Runs the complete standard pipeline for DDC only (reuses existing implementation).
# Metrics computed:
#   1.  Perfect Chats / Fully Handled By Bot ΓåÆ BOT_HANDLED_COUNT
#   2.  Chats Supposed to be Bot-Handled     ΓåÆ CHATS_SUPPOSED_TO_BE_BOT_HANDLED
#   3.  Call Request                         ΓåÆ CALL_REQUESTS_COUNT
#   4.  Chats with ΓëÑ1 Agent Message          ΓåÆ CHATS_WITH_1_PLUS_AGENT_MESSAGES
#       ΓööΓöÇ At Least 2 Agent Messages         ΓåÆ CHATS_WITH_2_PLUS_AGENT_MESSAGES
#       ΓööΓöÇ At Least 3 Agent Messages         ΓåÆ CHATS_WITH_3_PLUS_AGENT_MESSAGES
#       ΓööΓöÇ Exactly 2 Agent Messages          ΓåÆ CHATS_WITH_EXACTLY_2_AGENT_MESSAGES
#   5.  Technical API Failure                ΓåÆ TECH_ERROR_TRANSFERS_COUNT
#   6.  Avg Delay ΓÇô Initial Msg              ΓåÆ METHOD1_AVG_INITIAL_DELAY_SECONDS
#   7.  Avg Delay ΓÇô Non-Initial Msg          ΓåÆ METHOD1_AVG_NON_INITIAL_DELAY_SECONDS
#
# NOTE: Item #2 "Total Transfers %" is pending ΓÇö routing logic to be provided.

# Temporarily override globals for DDC-only run
_prev_dept_filter = globals().get('DEPARTMENT_FILTER', None)
_prev_test        = globals().get('TEST', False)
MASTER_METRICS_TABLE = globals().get('MASTER_METRICS_TABLE', 'EVALS_SUMMARY_2_test_only')

DEPARTMENT_FILTER = "DDC"
TEST = True

target_date_ddc = (datetime.now() - timedelta(days=1)).strftime('%Y-%m-%d')
print(f"≡ƒÜÇ Running analytics pipeline for: {DEPARTMENT_FILTER}")
print(f"≡ƒôà Target date: {target_date_ddc}")
print("=" * 55)

try:
    result_ddc = phase3_advanced_analytics_processor(session, target_date_ddc)
    print(result_ddc['summary'])
finally:
    # Always restore globals so the full pipeline is not affected
    DEPARTMENT_FILTER = _prev_dept_filter
    TEST = _prev_test

